import { Link } from "react-router-dom";

const ToTop = () => {
  return (
    <Link to="#" className="btn btn-lg btn-secondary btn-lg-square back-to-top">
      <i className="bi bi-arrow-up"></i>
    </Link>
  );
};

export default ToTop;
